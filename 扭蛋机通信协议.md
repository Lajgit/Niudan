# 扭蛋机通信协议

> 版本：V1.0  
> 定稿日期：2026-08-14  
> 适用固件：Niudan `main` 分支  
> 本文档以当前 MCU 代码实际实现为准，是扭蛋机主板与安卓之间的正式通信协议。

## 1. 通信接口

主板与安卓使用 USART1：

- 波特率：115200
- 数据位：8 bit
- 停止位：1 bit
- 校验位：None
- 硬件流控：None
- MCU 引脚：PA9=TX，PA10=RX

USART3 为主板与控台之间的内部通信接口，不作为安卓主协议接口。

## 2. 固定帧格式

每帧固定 14 字节：

| 字节 | 字段 | 说明 |
|---|---|---|
| 0 | Head | 固定 `0xAA` |
| 1 | ResendID | 重发次数，首次发送为 `0x00` |
| 2 | ID | 消息 ID，新消息自动递增，8 bit 循环 |
| 3 | Code1 | 通信方向 |
| 4 | Code2 | 功能码 |
| 5 | Data1 | 32 bit 数据最高字节 |
| 6 | Data2 | 数据字节 |
| 7 | Data3 | 数据字节 |
| 8 | Data4 | 32 bit 数据最低字节 |
| 9 | ACKbyte | `0x00`=不进入重发列表，`0x01`=需要对端应答 |
| 10 | ExpandCode | 补充类型/状态 |
| 11 | CRC16_H | CRC16 高字节 |
| 12 | CRC16_L | CRC16 低字节 |
| 13 | Tail | 固定 `0x55` |

32 bit 数据采用大端排列：

```text
Data = Data1<<24 | Data2<<16 | Data3<<8 | Data4
```

### 2.1 Code1 方向

| Code1 | 方向 |
|---|---|
| `0x00` | 主板 → 安卓 |
| `0x01` | 安卓 → 主板 |
| `0x02` | 主板 → 控台 |
| `0x03` | 控台 → 主板 |

### 2.2 CRC16

CRC 计算范围为 Byte0~Byte10，共 11 字节。

- 初始值：`0xFFFF`
- 多项式：`0xA001`
- 结果发送顺序：**高字节在前，低字节在后**

即：

```text
Byte11 = CRC >> 8
Byte12 = CRC & 0xFF
```

虽然算法与常见 Modbus CRC16 相同，但本协议在线上传输时使用高字节在前。

## 3. 应答与重发规则

### 3.1 安卓命令的应答

主板收到 CRC 正确且 `Code1=0x01` 的安卓命令后，先将原 14 字节消息内容回发作为应答，然后再执行业务逻辑。

应答包保持原消息的 `Code1/Code2/Data/ID/ExpandCode`，CRC 重新计算。

### 3.2 主板主动消息的应答

主板发送 `ACKbyte=0x01` 的消息时，安卓必须将收到的整帧原样回发。主板按 `ID` 从重发列表中删除该消息。

重发参数：

- 首次发送：`ResendID=0x00`
- 1 秒未收到应答：重发
- 最大重发次数：3 次
- 重发时 `ResendID` 依次为 `0x01/0x02/0x03`
- 同一消息 ID 的防重复执行窗口：250 ms

## 4. 安卓 → 主板正式命令

### 4.1 功能码总表

| Code2 | 功能 | Data | ExpandCode |
|---|---|---|---|
| `0x00` | 获取版本 | 0 | `0x00` |
| `0x01` | 输出扭蛋/钢珠 | Data3~Data4=数量 | `0x00`扭蛋，`0x01`钢珠 |
| `0x02` | 输出卡片 | Data3~Data4=数量 | `0x00` |
| `0x0B` | 清空扭蛋/钢珠 | 忽略 | `0x00`扭蛋，`0x01`钢珠 |
| `0x0C` | 继续当前剩余物品输出 | 忽略 | `0x00` |
| `0x0D` | 恢复默认设置 | 忽略 | `0x00` |
| `0x0E` | 保存设置 | 忽略 | `0x00` |
| `0x10` | 开锁 | 忽略 | `0x00` |
| `0x16` | 设置四位数码管 | Data1~Data4=整数值 | `0x00` |
| `0x18` | 设置控台亮度 | Data4=亮度 | `0x00` |
| `0x20` | 舵机1归零 | 忽略 | `0x00` |
| `0xF0` | 系统复位/进入 Bootloader | 见 4.7 | `0x00` |
| `0xFF` | 停止主要输出 | 忽略 | `0x00` |

未列入本表的旧弹界功能码，即使仍在头文件中保留数值，也不属于扭蛋机正式对外协议。

### 4.2 `0x00` 获取版本

安卓发送：

```text
Code1 = 0x01
Code2 = 0x00
```

主板随后主动发送：

```text
Code1 = 0x00
Code2 = 0x00
Data  = VERSION
```

当前固件版本为 `1.0.0.0`，对应：

```text
Data1=01 Data2=00 Data3=00 Data4=00
```

### 4.3 `0x01` 输出扭蛋/钢珠

数量只读取 Data3~Data4：

```text
数量 = Data3<<8 | Data4
```

ExpandCode 固定：

| ExpandCode | 通道 | 固件对象 |
|---|---|---|
| `0x00` | 扭蛋 | Motor_Hoolle2 |
| `0x01` | 钢珠 | Motor_Hoolle1 |

其他 ExpandCode 不执行出货动作。

例如：ID=1，输出 1 个扭蛋：

```text
AA 00 01 01 01 00 00 00 01 00 00 62 A8 55
```

### 4.4 `0x02` 输出卡片

```text
数量 = Data3<<8 | Data4
```

ExpandCode 固定 `0x00`。

### 4.5 `0x0B` 清空扭蛋/钢珠

ExpandCode 与 `0x01` 完全一致：

```text
0x00 = 扭蛋
0x01 = 钢珠
```

该命令进入清空模式并持续运行对应出货通道，直到空仓超时结束。

### 4.6 `0x16` 设置数码管

Data1~Data4 按 32 bit 无符号整数解析，显示低四位十进制数。

例如显示 `1234`：

```text
Data = 0x000004D2
Data1=00 Data2=00 Data3=04 Data4=D2
```

不是 ASCII `31 32 33 34`。

四位对应关系：

```text
SEG1 = 千位
SEG2 = 百位
SEG3 = 十位
SEG4 = 个位
```

### 4.7 `0xF0` 系统复位/进入 Bootloader

普通系统复位：Data 不等于 `0x424F5441`。

进入串口升级 Bootloader：

```text
Data1=42 Data2=4F Data3=54 Data4=41
```

即 ASCII `BOTA`，32 bit 值 `0x424F5441`。

主板先回命令应答，约 100 ms 后复位。

## 5. 主板 → 安卓正式消息

### 5.1 功能码总表

| Code2 | 功能 | Data | ExpandCode | ACKbyte |
|---|---|---|---|---|
| `0x00` | 版本信息 | 固件版本 | `0x00` | `0x00` |
| `0x01` | 投入珠子 | 0 | `0x00` | `0x01` |
| `0x02` | 投入硬币 | 0 | `0x00` | `0x01` |
| `0x03` | 拍拍/游玩按键 | Data4=编号 | 按键状态 | `0x00` |
| `0x04` | 后台设置按键 | Data4=编号 | 按键状态 | `0x00` |
| `0x05` | 剩余待出数量 | 当前剩余待出数量 | 物品类型 | `0x00` |
| `0x07` | 扭蛋/钢珠出货超时 | 当前剩余待出数量 | 出货类型 | `0x01` |
| `0x08` | 卡片输出超时 | 当前剩余待出卡数 | `0x00` | `0x01` |
| `0x0D` | 已开锁 | 0 | `0x00` | `0x00` |
| `0x0F` | 编码器事件 | 0 | 方向 | `0x00` |
| `0x11` | 清除剩余卡片提示 | 0 | `0x00` | `0x00` |
| `0x15` | 控台键盘转发兼容消息 | Data4=编号 | 状态 | `0x00` |

### 5.2 `0x03` 拍拍/游玩按键

当前物理按键：

| 按键编号 | MCU |
|---|---|
| `0x01` | PD8 |
| `0x02` | PD9 |
| `0x03` | PD10 |

帧关键字段：

```text
Code1      = 0x00
Code2      = 0x03
Data4      = 0x01~0x03
ACKbyte    = 0x00
ExpandCode = 按键状态
```

按键状态：

| ExpandCode | 状态 |
|---|---|
| `0x01` | 短按 |
| `0x02` | 长按 |
| `0x03` | 松开 |

例如 ID=1，1 号游玩按键短按：

```text
AA 00 01 00 03 00 00 00 01 00 01 AE 8B 55
```

### 5.3 `0x04` 后台设置按键

| 按键编号 | 原理图按键 | MCU |
|---|---|---|
| `0x01` | K1 | PB6 |
| `0x02` | K2 | PB7 |
| `0x03` | K3 | PD0 |

ExpandCode 与游玩按键一致：

```text
0x01 = 短按
0x02 = 长按
0x03 = 松开
```

### 5.4 `0x05` 剩余待出数量

本功能码表示**当前这次出货任务还剩多少个待输出**，不是机器实际库存总量。

为兼容已有卡片协议，ExpandCode 正式固定如下：

| ExpandCode | 类型 | Data 来源 |
|---|---|---|
| `0x00` | 扭蛋 | Motor_Hoolle2.Hoolle_num |
| `0x01` | 卡片 | Card.Card_num |
| `0x02` | 钢珠 | Motor_Hoolle1.Hoolle_num |

每成功输出一个对应物品后，主板将剩余待出数量减 1，再发送 `0x05`。

因此最后一个成功输出后会发送数量 `0`。

### 5.5 `0x07` 扭蛋/钢珠出货超时

该消息要求安卓应答：`ACKbyte=0x01`。

ExpandCode 正式固定：

| ExpandCode | 超时通道 | Data |
|---|---|---|
| `0x00` | 扭蛋 | Motor_Hoolle2 当前剩余待出数量 |
| `0x01` | 钢珠 | Motor_Hoolle1 当前剩余待出数量 |

扭蛋和钢珠不再共用错误的 Motor2 数量。

### 5.6 `0x08` 卡片输出超时

```text
Code1      = 0x00
Code2      = 0x08
Data       = Card.Card_num
ACKbyte    = 0x01
ExpandCode = 0x00
```

## 6. 投珠与投币

### 6.1 `0x01` 投入珠子

主板检测到进珠光眼一次有效事件后发送：

```text
Code1      = 0x00
Code2      = 0x01
Data       = 0
ACKbyte    = 0x01
ExpandCode = 0x00
```

安卓必须应答。

### 6.2 `0x02` 投入硬币

主板检测到一次有效投币脉冲后发送：

```text
Code1      = 0x00
Code2      = 0x02
Data       = 0
ACKbyte    = 0x01
ExpandCode = 0x00
```

安卓必须应答。

当前协议只上报投币事件，不携带币值。

## 7. 内部 USART3 控台协议

USART3 同样使用 14 字节帧，115200 8N1。

控台 → 主板使用 `Code1=0x03`：

| Code2 | 功能 |
|---|---|
| `0x01` | 控台按键，主板转发成安卓 `Code2=0x03` |
| `0x02` | 控台键盘，主板转发成安卓 `Code2=0x15` |
| `0x03` | 编码器/舵机调整 |

安卓发送 `Code2=0x18` 设置控台亮度时，主板向 USART3 发送：

```text
Code1 = 0x02
Code2 = 0x04
Data4 = 亮度
```

## 8. 正式类型值汇总

### 出货命令 `0x01`、清空命令 `0x0B`、超时消息 `0x07`

```text
0x00 = 扭蛋
0x01 = 钢珠
```

### 剩余待出数量 `0x05`

```text
0x00 = 扭蛋
0x01 = 卡片
0x02 = 钢珠
```

### 按键 `0x03/0x04`

```text
0x01 = 短按
0x02 = 长按
0x03 = 松开
```

## 9. 联调注意事项

1. 所有正式帧固定 14 字节，帧头 `0xAA`，帧尾 `0x55`。
2. CRC 只计算前 11 字节，CRC 高字节先发送。
3. `Data1~Data4` 是二进制整数，不是 ASCII。
4. 安卓收到 `ACKbyte=0x01` 的主板主动消息时必须原样回包。
5. 安卓发送命令后，主板会先原样应答命令，再执行对应动作。
6. `0x05` 是当前任务剩余待出数量，不表示实际库存。
7. 扭蛋固定对应 Motor_Hoolle2，钢珠固定对应 Motor_Hoolle1，后续代码不得交换这两个通道含义。
