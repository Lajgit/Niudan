# 扭蛋机通信协议

> 版本：V1.3  
> 定稿日期：2026-09-05  
> 适用固件：Niudan `main` 分支  
> 本文档以当前 MCU 代码实际实现为准，是扭蛋机主板与安卓之间的正式通信协议。

## 1. 当前硬件通信架构

当前设备**没有独立控台**。

- 主板与安卓：USART1，PA9=TX，PA10=RX。
- 游玩/拍拍按键：PD8、PD9、PD10 直接接主板 GPIO。
- 后台设置按键：PB6、PB7、PD0 直接接主板 GPIO。
- 四位数码管：主板通过 SPI2 直接驱动外接数码管模块；PB13=SCK、PB15=MOSI、PB12=RCLK、PB14=OE。
- 两路 12V LED 输出：每路硬件并接 3 个 LED 接口，由安卓 `0x0A` 命令控制开关。
- 旧控台按键、控台键盘、控台编码器、控台亮度、控台灯效及 USART3 控台协议均不再属于当前运行架构。
- USART3 底层驱动代码暂保留为备用，但当前主程序不初始化、通信任务不使用，不属于本协议。

USART1 参数：

- 波特率：115200
- 数据位：8 bit
- 停止位：1 bit
- 校验位：None
- 硬件流控：None

## 2. 固定帧格式

每帧固定 14 字节：

| 字节 | 字段 | 说明 |
|---|---|---|
| 0 | Head | 固定 `0xAA` |
| 1 | ResendID | 重发次数，首次发送为 `0x00` |
| 2 | ID | 消息 ID，新消息自动递增，8 bit 循环 |
| 3 | Code1 | 通信方向 |
| 4 | Code2 | 功能码 |
| 5 | Data1 | 32 bit 数据最高字节 |
| 6 | Data2 | 数据字节 |
| 7 | Data3 | 数据字节 |
| 8 | Data4 | 32 bit 数据最低字节 |
| 9 | ACKbyte | `0x00`=不进入重发列表，`0x01`=需要对端应答 |
| 10 | ExpandCode | 补充类型/状态 |
| 11 | CRC16_H | CRC16 高字节 |
| 12 | CRC16_L | CRC16 低字节 |
| 13 | Tail | 固定 `0x55` |

32 bit 数据采用大端排列：

```text
Data = Data1<<24 | Data2<<16 | Data3<<8 | Data4
```

### 2.1 Code1 方向

当前正式协议只使用：

| Code1 | 方向 |
|---|---|
| `0x00` | 主板 → 安卓 |
| `0x01` | 安卓 → 主板 |

旧控台使用过的 `0x02/0x03` 已退出当前协议。

### 2.2 CRC16

CRC 计算范围为 Byte0~Byte10，共 11 字节。

- 初始值：`0xFFFF`
- 多项式：`0xA001`
- 结果发送顺序：**高字节在前，低字节在后**

```text
Byte11 = CRC >> 8
Byte12 = CRC & 0xFF
```

## 3. 应答与重发规则

### 3.1 安卓命令应答

主板收到 CRC 正确且 `Code1=0x01` 的安卓命令后，先将原 14 字节消息内容回发作为应答，再执行业务逻辑。

应答包保持原消息的 `Code1/Code2/Data/ID/ExpandCode`，CRC 重新计算。

### 3.2 主板主动消息应答

主板发送 `ACKbyte=0x01` 的消息时，安卓必须将收到的整帧原样回发。主板按 `ID` 从重发列表中删除该消息。

重发参数：

- 首次发送：`ResendID=0x00`
- 1 秒未收到应答：重发
- 最大重发次数：3 次
- 重发时 `ResendID=0x01/0x02/0x03`
- 同一消息 ID 防重复执行窗口：250 ms

## 4. 安卓 → 主板正式命令

| Code2 | 功能 | Data | ExpandCode |
|---|---|---|---|
| `0x00` | 获取版本 | 0 | `0x00` |
| `0x01` | 输出扭蛋/钢珠 | Data3~Data4=数量 | `0x00`扭蛋，`0x01`钢珠 |
| `0x02` | 输出卡片 | Data3~Data4=数量 | `0x00` |
| `0x0A` | 灯带控制 | Data4=灯带路数 | `0x00`关，`0x01`开 |
| `0x0B` | 清空扭蛋/钢珠 | 忽略 | `0x00`扭蛋，`0x01`钢珠 |
| `0x0C` | 继续当前扭蛋/卡片剩余输出 | 忽略 | `0x00` |
| `0x0D` | 恢复默认设置 | 忽略 | `0x00` |
| `0x0E` | 保存设置 | 忽略 | `0x00` |
| `0x10` | 开锁 | 忽略 | `0x00` |
| `0x16` | 设置四位数码管 | Data1~Data4=整数值 | `0x00` |
| `0x20` | 舵机1归零 | 忽略 | `0x00` |
| `0x21` | 吐珠/钢珠电机手动开关 | Data1~Data4固定为0 | `0x00`关闭，`0x01`开启常转 |
| `0xF0` | 系统复位/进入 Bootloader | 见下文 | `0x00` |
| `0xFF` | 停止主要输出 | 忽略 | `0x00` |

旧控台命令 `0x18`（控台亮度）已删除，不再处理。

### 4.1 `0x00` 获取版本

安卓发送：

```text
Code1 = 0x01
Code2 = 0x00
```

主板随后主动发送：

```text
Code1 = 0x00
Code2 = 0x00
Data  = VERSION
```

当前固件版本为 `1.0.0.0`：

```text
Data1=01 Data2=00 Data3=00 Data4=00
```

### 4.2 `0x01` 输出扭蛋/钢珠

数量：

```text
数量 = Data3<<8 | Data4
```

类型固定：

| ExpandCode | 通道 | 固件对象 |
|---|---|---|
| `0x00` | 扭蛋 | Motor_Hoolle2 |
| `0x01` | 钢珠 | Motor_Hoolle1 |

其他 ExpandCode 不执行。

### 4.3 `0x02` 输出卡片

```text
数量 = Data3<<8 | Data4
ExpandCode = 0x00
```

### 4.4 `0x0A` 灯带控制

该命令控制两路 12V LED 输出，每路硬件对应 3 个 LED 接口。

固定字段：

| 字段 | 值 |
|---|---|
| Head | `0xAA` |
| ResendID | 首次 `0x00` |
| ID | 安卓消息ID |
| Code1 | `0x01` |
| Code2 | `0x0A` |
| Data1 | `0x00` |
| Data2 | `0x00` |
| Data3 | `0x00` |
| Data4 | `0x01`第1路三组LED，`0x02`第2路三组LED |
| ACKbyte | `0x01` |
| ExpandCode | `0x00`关闭，`0x01`开启 |
| CRC16_H/L | 对 Byte0~Byte10 计算 |
| Tail | `0x55` |

帧格式：

```text
AA ResendID ID 01 0A 00 00 00 Data4 01 ExpandCode CRC_H CRC_L 55
```

示例：

```text
Data4=0x01，ExpandCode=0x01 → 打开第1路三组LED
Data4=0x01，ExpandCode=0x00 → 关闭第1路三组LED
Data4=0x02，ExpandCode=0x01 → 打开第2路三组LED
Data4=0x02，ExpandCode=0x00 → 关闭第2路三组LED
```

`Data4` 除 `0x01/0x02` 外不执行动作；`ExpandCode` 除 `0x00/0x01` 外不执行动作。

### 4.5 `0x0B` 清空扭蛋/钢珠

```text
ExpandCode=0x00 → 扭蛋
ExpandCode=0x01 → 钢珠
```

### 4.6 `0x16` 设置四位数码管

Data1~Data4 按 32 bit 无符号整数解析，显示低四位十进制数。

例如显示 `1234`：

```text
Data = 0x000004D2
Data1=00 Data2=00 Data3=04 Data4=D2
```

不是 ASCII `31 32 33 34`。

位序固定：

```text
SEG1 = 千位
SEG2 = 百位
SEG3 = 十位
SEG4 = 个位
```

数码管由主板 SPI2 直接刷新，不经过任何控台转发。

### 4.7 `0x21` 吐珠/钢珠电机手动开关

该命令只控制 **Motor_Hoolle1（吐珠/钢珠电机）**，不控制扭蛋电机 Motor_Hoolle2。

固定字段：

| 字段 | 值 |
|---|---|
| Head | `0xAA` |
| ResendID | 首次 `0x00` |
| ID | 安卓消息ID |
| Code1 | `0x01` |
| Code2 | `0x21` |
| Data1 | `0x00` |
| Data2 | `0x00` |
| Data3 | `0x00` |
| Data4 | `0x00` |
| ACKbyte | `0x01` |
| ExpandCode | `0x00`关闭，`0x01`开启 |
| CRC16_H/L | 对 Byte0~Byte10 计算 |
| Tail | `0x55` |

帧格式：

```text
AA ResendID ID 01 21 00 00 00 00 01 ExpandCode CRC_H CRC_L 55
```

关闭：

```text
ExpandCode = 0x00
```

主板立即停止 Motor_Hoolle1，并退出手动常转模式。

开启：

```text
ExpandCode = 0x01
```

主板立即以 `HoolleMotor_Speed=100%`、正常方向启动 Motor_Hoolle1，并持续运行，直到收到以下任一条件：

- `0x21 / ExpandCode=0x00`；
- `0xFF` 停止所有设备；
- 系统复位或进入 Bootloader。

手动常转模式与正常按数量出货逻辑隔离：

- 开启时清除 Motor_Hoolle1 原有 `Hoolle_num`、`RetryCount` 和 `ClearMode`；
- 不读取珠数作为停止条件；
- 不执行 3 秒出货超时判断；
- 不执行超时反转；
- 不执行超时重试；
- 不发送由该手动运行产生的 `0x05` 钢珠剩余数量消息；
- 不发送由该手动运行产生的 `0x07` 钢珠出货超时消息；
- 手动常转期间收到正常钢珠数量出货命令时，不改变当前手动常转状态。

`ExpandCode` 除 `0x00/0x01` 外的值不执行动作。

### 4.8 `0xF0` 系统复位/进入 Bootloader

普通系统复位：Data 不等于 `0x424F5441`。

进入串口升级 Bootloader：

```text
Data1=42 Data2=4F Data3=54 Data4=41
```

即 ASCII `BOTA`，32 bit 值 `0x424F5441`。主板先回命令应答，约 100 ms 后复位。

## 5. 主板 → 安卓正式消息

| Code2 | 功能 | Data | ExpandCode | ACKbyte |
|---|---|---|---|---|
| `0x00` | 版本信息 | 固件版本 | `0x00` | `0x00` |
| `0x01` | 投入珠子 | 0 | `0x00` | `0x01` |
| `0x02` | 投入硬币 | 0 | `0x00` | `0x01` |
| `0x03` | 游玩/拍拍按键 | Data4=编号 | 按键状态 | `0x00` |
| `0x04` | 后台设置按键 | Data4=编号 | 按键状态 | `0x00` |
| `0x05` | 剩余待出数量 | 当前剩余待出数量 | 物品类型 | `0x00` |
| `0x07` | 扭蛋/钢珠出货超时 | 当前剩余待出数量 | 出货类型 | `0x01` |
| `0x08` | 卡片输出超时 | 当前剩余待出卡数 | `0x00` | `0x01` |
| `0x0D` | 已开锁 | 0 | `0x00` | `0x00` |
| `0x11` | 清除剩余卡片提示 | 0 | `0x00` | `0x00` |

旧控台转发消息 `0x0F`（编码器）和 `0x15`（控台键盘）已退出正式协议。

### 5.1 `0x03` 游玩/拍拍按键

当前三个按键全部直接接主板：

| 编号 | MCU |
|---|---|
| `0x01` | PD8 |
| `0x02` | PD9 |
| `0x03` | PD10 |

```text
Code1      = 0x00
Code2      = 0x03
Data4      = 0x01~0x03
ACKbyte    = 0x00
ExpandCode = 按键状态
```

按键状态：

```text
0x01 = 短按
0x02 = 长按
0x03 = 松开
```

### 5.2 `0x04` 后台设置按键

| 编号 | 原理图按键 | MCU |
|---|---|---|
| `0x01` | K1 | PB6 |
| `0x02` | K2 | PB7 |
| `0x03` | K3 | PD0 |

ExpandCode 与游玩按键一致：

```text
0x01 = 短按
0x02 = 长按
0x03 = 松开
```

### 5.3 `0x05` 剩余待出数量

该值表示**当前出货任务剩余待输出数量**，不是机器实际库存。

| ExpandCode | 类型 | Data 来源 |
|---|---|---|
| `0x00` | 扭蛋 | Motor_Hoolle2.Hoolle_num |
| `0x01` | 卡片 | Card.Card_num |
| `0x02` | 钢珠 | Motor_Hoolle1.Hoolle_num |

### 5.4 `0x07` 扭蛋/钢珠出货超时

该消息 `ACKbyte=0x01`，安卓必须应答。

| ExpandCode | 超时通道 | Data |
|---|---|---|
| `0x00` | 扭蛋 | Motor_Hoolle2 当前剩余待出数量 |
| `0x01` | 钢珠 | Motor_Hoolle1 当前剩余待出数量 |

### 5.5 `0x08` 卡片输出超时

```text
Code1      = 0x00
Code2      = 0x08
Data       = Card.Card_num
ACKbyte    = 0x01
ExpandCode = 0x00
```

## 6. 投珠与投币

### `0x01` 投入珠子

```text
Code1      = 0x00
Code2      = 0x01
Data       = 0
ACKbyte    = 0x01
ExpandCode = 0x00
```

### `0x02` 投入硬币

```text
Code1      = 0x00
Code2      = 0x02
Data       = 0
ACKbyte    = 0x01
ExpandCode = 0x00
```

当前只上报投币事件，不携带币值。

## 7. 正式类型值汇总

出货命令 `0x01`、清空命令 `0x0B`、出货超时 `0x07`：

```text
0x00 = 扭蛋
0x01 = 钢珠
```

剩余待出数量 `0x05`：

```text
0x00 = 扭蛋
0x01 = 卡片
0x02 = 钢珠
```

按键 `0x03/0x04`：

```text
0x01 = 短按
0x02 = 长按
0x03 = 松开
```

灯带控制 `0x0A`：

```text
Data4:
0x01 = 第1路三组LED
0x02 = 第2路三组LED

ExpandCode:
0x00 = 关闭
0x01 = 开启
```

吐珠电机手动开关 `0x21`：

```text
0x00 = 关闭
0x01 = 开启并持续运行
```

## 8. 联调约束

1. 所有正式帧固定 14 字节，帧头 `0xAA`，帧尾 `0x55`。
2. CRC 只计算前 11 字节，CRC 高字节先发送。
3. `Data1~Data4` 是二进制整数，不是 ASCII。
4. 安卓收到 `ACKbyte=0x01` 的主板主动消息时必须原样回包。
5. 安卓发送命令后，主板会先原样应答命令，再执行对应动作。
6. `0x05` 表示当前任务剩余待出数量，不表示实际库存。
7. 扭蛋固定对应 Motor_Hoolle2，钢珠/吐珠固定对应 Motor_Hoolle1。
8. `0x0A` 只控制两路 12V LED 输出，`Data4=0x01/0x02` 分别对应第1/第2路三组LED。
9. `0x21` 是 Motor_Hoolle1 的独立手动常转控制，不得与 `0x01` 按数量出货混用为同一种状态机。
10. 当前不存在控台；安卓不得再使用旧控台亮度、控台键盘、编码器转发或 USART3 控台协议。
